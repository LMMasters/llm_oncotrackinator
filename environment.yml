name: oncotrack
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pip
  - pip:
      - pandas>=1.3.0
      - pydantic>=2.0.0
      - ollama>=0.1.0
      - python-dateutil>=2.8.0
